<?php
/**
 * MUImage.
 *
 * @copyright Michael Ueberschaer (MU)
 * @license http://www.gnu.org/licenses/lgpl.html GNU Lesser General Public License
 * @author Michael Ueberschaer <kontakt@webdesign-in-bremen.com>.
 * @link http://www.webdesign-in-bremen.com
 * @link http://zikula.org
 * @version Generated by ModuleStudio 0.7.0 (http://modulestudio.de).
 */

namespace MU\MUImageModule\Entity\Base;

use MU\MUImageModule\MUImageEvents;
use MU\MUImageModule\Event\FilterAlbumEvent;
use Doctrine\ORM\Mapping as ORM;
use Doctrine\Common\Collections\ArrayCollection;
use Gedmo\Mapping\Annotation as Gedmo;
use DoctrineExtensions\StandardFields\Mapping\Annotation as ZK;
use Symfony\Component\Validator\Constraints as Assert;
use Symfony\Bridge\Doctrine\Validator\Constraints\UniqueEntity;

use DataUtil;
use FormUtil;
use RuntimeException;
use ServiceUtil;
use UserUtil;
use Zikula_Workflow_Util;
use Zikula\Core\Doctrine\EntityAccess;

/**
 * Entity class that defines the entity structure and behaviours.
 *
 * This is the base entity class for album entities.
 * The following annotation marks it as a mapped superclass so subclasses
 * inherit orm properties.
 *
 * @ORM\MappedSuperclass
 *
 * @abstract
 */
abstract class AbstractAlbumEntity extends EntityAccess
{
    /**
     * @var string The tablename this object maps to.
     */
    protected $_objectType = 'album';
    
    /**
     * @Assert\Type(type="bool")
     * @var boolean Option to bypass validation if needed.
     */
    protected $_bypassValidation = false;
    
    /**
     * @Assert\Type(type="array")
     * @var array List of available item actions.
     */
    protected $_actions = [];
    
    /**
     * @var array The current workflow data of this object.
     */
    protected $__WORKFLOW__ = [];
    
    /**
     * @ORM\Id
     * @ORM\GeneratedValue(strategy="AUTO")
     * @ORM\Column(type="integer", unique=true)
     * @var integer $id.
     */
    protected $id = 0;
    
    /**
     * the current workflow state
     * @ORM\Column(length=20)
     * @Assert\NotBlank()
     * @Assert\Choice(callback="getWorkflowStateAllowedValues", multiple=false)
     * @var string $workflowState.
     */
    protected $workflowState = 'initial';
    
    /**
     * @ORM\Column(length=255, unique=true)
     * @Assert\NotBlank()
     * @Assert\Length(min="0", max="255")
     * @var string $title.
     */
    protected $title = '';
    
    /**
     * @ORM\Column(type="text", length=2000)
     * @Assert\NotNull()
     * @Assert\Length(min="0", max="2000")
     * @var text $description.
     */
    protected $description = '';
    
    /**
     * @ORM\Column(type="integer", nullable=true)
     * @var integer $parent_id.
     */
    protected $parent_id = 0;
    
    /**
     * @ORM\Column(length=255)
     * @Assert\NotBlank()
     * @Assert\Choice(callback="getAlbumAccessAllowedValues", multiple=false)
     * @var string $albumAccess.
     */
    protected $albumAccess = null;
    
    /**
     * @ORM\Column(length=255)
     * @Assert\NotNull()
     * @Assert\Length(min="0", max="255")
     * @var string $passwordAccess.
     */
    protected $passwordAccess = '';
    
    /**
     * @ORM\Column(length=255)
     * @Assert\NotNull()
     * @Assert\Length(min="0", max="255")
     * @var string $myFriends.
     */
    protected $myFriends = '';
    
    /**
     * @ORM\Column(type="boolean")
     * @Assert\NotNull()
     * @Assert\Type(type="bool")
     * @var boolean $notInFrontend.
     */
    protected $notInFrontend = false;
    
    
    /**
     * @ORM\OneToMany(targetEntity="\MU\MUImageModule\Entity\AlbumCategoryEntity", 
     *                mappedBy="entity", cascade={"all"}, 
     *                orphanRemoval=true)
     * @var \MU\MUImageModule\Entity\AlbumCategoryEntity
     */
    protected $categories = null;
    
    /**
     * @ORM\Column(type="integer")
     * @ZK\StandardFields(type="userid", on="create")
     * @Assert\Type(type="integer")
     * @var integer $createdUserId.
     */
    protected $createdUserId;
    
    /**
     * @ORM\Column(type="integer")
     * @ZK\StandardFields(type="userid", on="update")
     * @Assert\Type(type="integer")
     * @var integer $updatedUserId.
     */
    protected $updatedUserId;
    
    /**
     * @ORM\Column(type="datetime")
     * @Gedmo\Timestampable(on="create")
     * @Assert\DateTime()
     * @var \DateTime $createdDate.
     */
    protected $createdDate;
    
    /**
     * @ORM\Column(type="datetime")
     * @Gedmo\Timestampable(on="update")
     * @Assert\DateTime()
     * @var \DateTime $updatedDate.
     */
    protected $updatedDate;
    
    /**
     * Bidirectional - Many children [albums] are linked by one parent [album] (OWNING SIDE).
     *
     * @ORM\ManyToOne(targetEntity="MU\MUImageModule\Entity\AlbumEntity", inversedBy="children")
     * @ORM\JoinTable(name="mu_muimage_album",
     *      joinColumns={@ORM\JoinColumn(name="id", referencedColumnName="id" )},
     *      inverseJoinColumns={@ORM\JoinColumn(name="parent_id", referencedColumnName="id" )}
     * )
     * @Assert\Type(type="MU\MUImageModule\Entity\AlbumEntity")
     * @Assert\Valid()
     * @var \MU\MUImageModule\Entity\AlbumEntity $parent.
     */
    protected $parent;
    
    /**
     * Bidirectional - One album [album] has many picture [pictures] (INVERSE SIDE).
     *
     * @ORM\OneToMany(targetEntity="MU\MUImageModule\Entity\PictureEntity", mappedBy="album", cascade={"all"})
     * @ORM\JoinTable(name="mu_muimage_albumpicture")
     * @ORM\OrderBy({"pos" = "ASC"})
     * @var \MU\MUImageModule\Entity\PictureEntity[] $picture.
     */
    protected $picture = null;
    
    /**
     * Bidirectional - One parent [album] has many children [albums] (INVERSE SIDE).
     *
     * @ORM\OneToMany(targetEntity="MU\MUImageModule\Entity\AlbumEntity", mappedBy="parent", cascade={"remove"})
     * @ORM\JoinTable(name="mu_muimage_parentchildren",
     *      joinColumns={@ORM\JoinColumn(name="parent_id", referencedColumnName="id" )},
     *      inverseJoinColumns={@ORM\JoinColumn(name="id", referencedColumnName="id" )}
     * )
     * @var \MU\MUImageModule\Entity\AlbumEntity[] $children.
     */
    protected $children = null;
    
    
    /**
     * Constructor.
     * Will not be called by Doctrine and can therefore be used
     * for own implementation purposes. It is also possible to add
     * arbitrary arguments as with every other class method.
     *
     * @param TODO
     */
    public function __construct()
    {
        $serviceManager = ServiceUtil::getManager();
        
        $listHelper = $serviceManager->get('mu_muimage_module.listentries_helper');
        
        $items = [];
        $listEntries = $listHelper->getAlbumAccessEntriesForAlbum();
        foreach ($listEntries as $listEntry) {
            if ($listEntry['default'] === true) {
                $items[] = $listEntry['value'];
            }
        }
        $this->albumAccess = implode('###', $items);
        
        $this->workflowState = 'initial';
        $this->initWorkflow();
        $this->picture = new ArrayCollection();
        $this->children = new ArrayCollection();
        $this->categories = new ArrayCollection();
    }
    
    /**
     * Gets the _object type.
     *
     * @return string
     */
    public function get_objectType()
    {
        return $this->_objectType;
    }
    
    /**
     * Sets the _object type.
     *
     * @param string $_objectType.
     *
     * @return void
     */
    public function set_objectType($_objectType)
    {
        $this->_objectType = $_objectType;
    }
    
    /**
     * Gets the _bypass validation.
     *
     * @return boolean
     */
    public function get_bypassValidation()
    {
        return $this->_bypassValidation;
    }
    
    /**
     * Sets the _bypass validation.
     *
     * @param boolean $_bypassValidation.
     *
     * @return void
     */
    public function set_bypassValidation($_bypassValidation)
    {
        $this->_bypassValidation = $_bypassValidation;
    }
    
    /**
     * Gets the _actions.
     *
     * @return array
     */
    public function get_actions()
    {
        return $this->_actions;
    }
    
    /**
     * Sets the _actions.
     *
     * @param array $_actions.
     *
     * @return void
     */
    public function set_actions(array $_actions = [])
    {
        $this->_actions = $_actions;
    }
    
    /**
     * Gets the __ w o r k f l o w__.
     *
     * @return array
     */
    public function get__WORKFLOW__()
    {
        return $this->__WORKFLOW__;
    }
    
    /**
     * Sets the __ w o r k f l o w__.
     *
     * @param array $__WORKFLOW__.
     *
     * @return void
     */
    public function set__WORKFLOW__(array $__WORKFLOW__ = [])
    {
        $this->__WORKFLOW__ = $__WORKFLOW__;
    }
    
    
    /**
     * Gets the id.
     *
     * @return integer
     */
    public function getId()
    {
        return $this->id;
    }
    
    /**
     * Sets the id.
     *
     * @param integer $id.
     *
     * @return void
     */
    public function setId($id)
    {
        $this->id = $id;
    }
    
    /**
     * Gets the workflow state.
     *
     * @return string
     */
    public function getWorkflowState()
    {
        return $this->workflowState;
    }
    
    /**
     * Sets the workflow state.
     *
     * @param string $workflowState.
     *
     * @return void
     */
    public function setWorkflowState($workflowState)
    {
        $this->workflowState = $workflowState;
    }
    
    /**
     * Gets the title.
     *
     * @return string
     */
    public function getTitle()
    {
        return $this->title;
    }
    
    /**
     * Sets the title.
     *
     * @param string $title.
     *
     * @return void
     */
    public function setTitle($title)
    {
        $this->title = $title;
    }
    
    /**
     * Gets the description.
     *
     * @return text
     */
    public function getDescription()
    {
        return $this->description;
    }
    
    /**
     * Sets the description.
     *
     * @param text $description.
     *
     * @return void
     */
    public function setDescription($description)
    {
        $this->description = $description;
    }
    
    /**
     * Gets the parent_id.
     *
     * @return integer
     */
    public function getParent_id()
    {
        return $this->parent_id;
    }
    
    /**
     * Sets the parent_id.
     *
     * @param integer $parent_id.
     *
     * @return void
     */
    public function setParent_id($parent_id)
    {
        $this->parent_id = $parent_id;
    }
    
    /**
     * Gets the album access.
     *
     * @return string
     */
    public function getAlbumAccess()
    {
        return $this->albumAccess;
    }
    
    /**
     * Sets the album access.
     *
     * @param string $albumAccess.
     *
     * @return void
     */
    public function setAlbumAccess($albumAccess)
    {
        $this->albumAccess = $albumAccess;
    }
    
    /**
     * Gets the password access.
     *
     * @return string
     */
    public function getPasswordAccess()
    {
        return $this->passwordAccess;
    }
    
    /**
     * Sets the password access.
     *
     * @param string $passwordAccess.
     *
     * @return void
     */
    public function setPasswordAccess($passwordAccess)
    {
        $this->passwordAccess = $passwordAccess;
    }
    
    /**
     * Gets the my friends.
     *
     * @return string
     */
    public function getMyFriends()
    {
        return $this->myFriends;
    }
    
    /**
     * Sets the my friends.
     *
     * @param string $myFriends.
     *
     * @return void
     */
    public function setMyFriends($myFriends)
    {
        $this->myFriends = $myFriends;
    }
    
    /**
     * Gets the not in frontend.
     *
     * @return boolean
     */
    public function getNotInFrontend()
    {
        return $this->notInFrontend;
    }
    
    /**
     * Sets the not in frontend.
     *
     * @param boolean $notInFrontend.
     *
     * @return void
     */
    public function setNotInFrontend($notInFrontend)
    {
        if ($notInFrontend !== $this->notInFrontend) {
            $this->notInFrontend = (bool)$notInFrontend;
        }
    }
    
    /**
     * Gets the categories.
     *
     * @return array
     */
    public function getCategories()
    {
        return $this->categories;
    }
    
    /**
     * Sets the categories.
     *
     * @param array $categories.
     *
     * @return void
     */
    public function setCategories($categories)
    {
        $this->categories = $categories;
    }
    
    /**
     * Gets the created user id.
     *
     * @return integer
     */
    public function getCreatedUserId()
    {
        return $this->createdUserId;
    }
    
    /**
     * Sets the created user id.
     *
     * @param integer $createdUserId.
     *
     * @return void
     */
    public function setCreatedUserId($createdUserId)
    {
        $this->createdUserId = $createdUserId;
    }
    
    /**
     * Gets the updated user id.
     *
     * @return integer
     */
    public function getUpdatedUserId()
    {
        return $this->updatedUserId;
    }
    
    /**
     * Sets the updated user id.
     *
     * @param integer $updatedUserId.
     *
     * @return void
     */
    public function setUpdatedUserId($updatedUserId)
    {
        $this->updatedUserId = $updatedUserId;
    }
    
    /**
     * Gets the created date.
     *
     * @return \DateTime
     */
    public function getCreatedDate()
    {
        return $this->createdDate;
    }
    
    /**
     * Sets the created date.
     *
     * @param \DateTime $createdDate.
     *
     * @return void
     */
    public function setCreatedDate($createdDate)
    {
        $this->createdDate = $createdDate;
    }
    
    /**
     * Gets the updated date.
     *
     * @return \DateTime
     */
    public function getUpdatedDate()
    {
        return $this->updatedDate;
    }
    
    /**
     * Sets the updated date.
     *
     * @param \DateTime $updatedDate.
     *
     * @return void
     */
    public function setUpdatedDate($updatedDate)
    {
        $this->updatedDate = $updatedDate;
    }
    
    
    /**
     * Gets the parent.
     *
     * @return \MU\MUImageModule\Entity\AlbumEntity
     */
    public function getParent()
    {
        return $this->parent;
    }
    
    /**
     * Sets the parent.
     *
     * @param \MU\MUImageModule\Entity\AlbumEntity $parent.
     *
     * @return void
     */
    public function setParent(\MU\MUImageModule\Entity\AlbumEntity $parent = null)
    {
        $this->parent = $parent;
    }
    
    /**
     * Gets the picture.
     *
     * @return \MU\MUImageModule\Entity\PictureEntity[]
     */
    public function getPicture()
    {
        return $this->picture;
    }
    
    /**
     * Sets the picture.
     *
     * @param \MU\MUImageModule\Entity\PictureEntity[] $picture.
     *
     * @return void
     */
    public function setPicture($picture)
    {
        foreach ($picture as $pictureSingle) {
            $this->addPicture($pictureSingle);
        }
    }
    
    /**
     * Adds an instance of \MU\MUImageModule\Entity\PictureEntity to the list of picture.
     *
     * @param \MU\MUImageModule\Entity\PictureEntity $picture The instance to be added to the collection.
     *
     * @return void
     */
    public function addPicture(\MU\MUImageModule\Entity\PictureEntity $picture)
    {
        $this->picture->add($picture);
        $picture->setAlbum($this);
    }
    
    /**
     * Removes an instance of \MU\MUImageModule\Entity\PictureEntity from the list of picture.
     *
     * @param \MU\MUImageModule\Entity\PictureEntity $picture The instance to be removed from the collection.
     *
     * @return void
     */
    public function removePicture(\MU\MUImageModule\Entity\PictureEntity $picture)
    {
        $this->picture->removeElement($picture);
        $picture->setAlbum(null);
    }
    
    /**
     * Gets the children.
     *
     * @return \MU\MUImageModule\Entity\AlbumEntity[]
     */
    public function getChildren()
    {
        return $this->children;
    }
    
    /**
     * Sets the children.
     *
     * @param \MU\MUImageModule\Entity\AlbumEntity[] $children.
     *
     * @return void
     */
    public function setChildren($children)
    {
        foreach ($children as $albumSingle) {
            $this->addChildren($albumSingle);
        }
    }
    
    /**
     * Adds an instance of \MU\MUImageModule\Entity\AlbumEntity to the list of children.
     *
     * @param \MU\MUImageModule\Entity\AlbumEntity $album The instance to be added to the collection.
     *
     * @return void
     */
    public function addChildren(\MU\MUImageModule\Entity\AlbumEntity $album)
    {
        $this->children->add($album);
        $album->setParent($this);
    }
    
    /**
     * Removes an instance of \MU\MUImageModule\Entity\AlbumEntity from the list of children.
     *
     * @param \MU\MUImageModule\Entity\AlbumEntity $album The instance to be removed from the collection.
     *
     * @return void
     */
    public function removeChildren(\MU\MUImageModule\Entity\AlbumEntity $album)
    {
        $this->children->removeElement($album);
        $album->setParent(null);
    }
    
    
    protected $processedLoadCallback = false;
    
    /**
     * Post-Process the data after the entity has been constructed by the entity manager.
     * The event happens after the entity has been loaded from database or after a refresh call.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *     - no access to associations (not initialised yet)
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::postLoadCallback()
     * @return boolean true if completed successfully else false.
     *
     * @throws RuntimeException Thrown if upload file base path retrieval fails
     */
    protected function performPostLoadCallback()
    {
        // echo 'loaded a record ...';
        if ($this->processedLoadCallback) {
            return true;
        }
    
        $currentFunc = FormUtil::getPassedValue('func', 'index', 'GETPOST', FILTER_SANITIZE_STRING);
        $serviceManager = ServiceUtil::getManager();
        $requestStack = $serviceManager->get('request_stack');
        $usesCsvOutput = $requestStack->getCurrentRequest()->getRequestFormat() == 'csv' ? true : false;
        
        $this['id'] = (int) ((isset($this['id']) && !empty($this['id'])) ? DataUtil::formatForDisplay($this['id']) : 0);
        $this->formatTextualField('workflowState', $currentFunc, $usesCsvOutput, true);
        $this->formatTextualField('title', $currentFunc, $usesCsvOutput);
        $this->formatTextualField('description', $currentFunc, $usesCsvOutput);
        $this['parent_id'] = (int) ((isset($this['parent_id']) && !empty($this['parent_id'])) ? DataUtil::formatForDisplay($this['parent_id']) : 0);
        $this->formatTextualField('albumAccess', $currentFunc, $usesCsvOutput, true);
        $this->formatTextualField('passwordAccess', $currentFunc, $usesCsvOutput);
        $this->formatTextualField('myFriends', $currentFunc, $usesCsvOutput);
        $this['notInFrontend'] = (bool) $this['notInFrontend'];
    
        $this->prepareItemActions();
    
        $serviceManager = ServiceUtil::getManager();
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_POST_LOAD, $event);
    
        $this->processedLoadCallback = true;
    
        return true;
    }
    
    /**
     * Formats a given textual field depending on it's actual kind of content.
     *
     * @param string  $fieldName     Name of field to be formatted.
     * @param string  $currentFunc   Name of current controller action.
     * @param string  $usesCsvOutput Whether the output is CSV or not (defaults to false).
     * @param boolean $allowZero     Whether 0 values are allowed or not (defaults to false).
     */
    protected function formatTextualField($fieldName, $currentFunc, $usesCsvOutput = false, $allowZero = false)
    {
        if ($currentFunc == 'edit') {
            // apply no changes when editing the content
            return;
        }
    
        if ($usesCsvOutput == 1) {
            // apply no changes for CSV output
            return;
        }
    
        $string = '';
        if (isset($this[$fieldName])) {
            if (!empty($this[$fieldName]) || ($allowZero && $this[$fieldName] == 0)) {
                $string = $this[$fieldName];
                if ($this->containsHtml($string)) {
                    $string = DataUtil::formatForDisplayHTML($string);
                } else {
                    $string = DataUtil::formatForDisplay($string);
                    $string = nl2br($string);
                }
            }
        }
    
        // workaround for ampersand problem (#692)
        $string = str_replace('&amp;', '&', $string);
    
        $this[$fieldName] = $string;
    }
    
    /**
     * Checks whether any html tags are contained in the given string.
     * See http://stackoverflow.com/questions/10778035/how-to-check-if-string-contents-have-any-html-in-it for implementation details.
     *
     * @param $string string The given input string.
     *
     * @return boolean Whether any html tags are found or not.
     */
    protected function containsHtml($string)
    {
        return preg_match("/<[^<]+>/", $string, $m) != 0;
    }
    
    /**
     * Pre-Process the data prior to an insert operation.
     * The event happens before the entity managers persist operation is executed for this entity.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *     - no identifiers available if using an identity generator like sequences
     *     - Doctrine won't recognize changes on relations which are done here
     *       if this method is called by cascade persist
     *     - no creation of other entities allowed
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::prePersistCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPrePersistCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_PRE_PERSIST, $event);
        if ($event->isPropagationStopped()) {
            return false;
        }
    
        return true;
    }
    
    /**
     * Post-Process the data after an insert operation.
     * The event happens after the entity has been made persistant.
     * Will be called after the database insert operations.
     * The generated primary key values are available.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::postPersistCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPostPersistCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $objectId = $this->createCompositeIdentifier();
        $logger = $serviceManager->get('logger');
        $logArgs = ['app' => 'MUMUImageModule', 'user' => $serviceManager->get('zikula_users_module.current_user')->get('uname'), 'entity' => 'album', 'id' => $objectId];
        $logger->debug('{app}: User {user} created the {entity} with id {id}.', $logArgs);
    
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_POST_PERSIST, $event);
    
        return true;
    }
    
    /**
     * Pre-Process the data prior a delete operation.
     * The event happens before the entity managers remove operation is executed for this entity.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *     - will not be called for a DQL DELETE statement
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::preRemoveCallback()
     * @return boolean true if completed successfully else false.
     *
     * @throws RuntimeException Thrown if workflow deletion fails
     */
    protected function performPreRemoveCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_PRE_REMOVE, $event);
        if ($event->isPropagationStopped()) {
            return false;
        }
    
        // delete workflow for this entity
        $serviceManager = ServiceUtil::getManager();
        $workflowHelper = $serviceManager->get('mu_muimage_module.workflow_helper');
        $workflowHelper->normaliseWorkflowData($this);
        $workflow = $this['__WORKFLOW__'];
        if ($workflow['id'] > 0) {
            $entityManager = $serviceManager->get('doctrine.entitymanager');
            $result = true;
            try {
                $workflow = $entityManager->find('Zikula\Core\Doctrine\Entity\WorkflowEntity', $workflow['id']);
                $entityManager->remove($workflow);
                $entityManager->flush();
            } catch (\Exception $e) {
                $result = false;
            }
            if ($result === false) {
                $session = $serviceManager->get('session');
                $session->getFlashBag()->add(\Zikula_Session::MESSAGE_ERROR, $serviceManager->get('translator.default')->__('Error! Could not remove stored workflow. Deletion has been aborted.'));
    
                return false;
            }
        }
    
        return true;
    }
    
    /**
     * Post-Process the data after a delete.
     * The event happens after the entity has been deleted.
     * Will be called after the database delete operations.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *     - will not be called for a DQL DELETE statement
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::postRemoveCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPostRemoveCallback()
    {
        $serviceManager = ServiceUtil::getManager();
    
        $objectId = $this->createCompositeIdentifier();
    
    
        $logger = $serviceManager->get('logger');
        $logArgs = ['app' => 'MUMUImageModule', 'user' => $serviceManager->get('zikula_users_module.current_user')->get('uname'), 'entity' => 'album', 'id' => $objectId];
        $logger->debug('{app}: User {user} removed the {entity} with id {id}.', $logArgs);
    
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_POST_REMOVE, $event);
    
        return true;
    }
    
    /**
     * Pre-Process the data prior to an update operation.
     * The event happens before the database update operations for the entity data.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *     - will not be called for a DQL UPDATE statement
     *     - changes on associations are not allowed and won't be recognized by flush
     *     - changes on properties won't be recognized by flush as well
     *     - no creation of other entities allowed
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::preUpdateCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPreUpdateCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_PRE_UPDATE, $event);
        if ($event->isPropagationStopped()) {
            return false;
        }
    
        return true;
    }
    
    /**
     * Post-Process the data after an update operation.
     * The event happens after the database update operations for the entity data.
     *
     * Restrictions:
     *     - no access to entity manager or unit of work apis
     *     - will not be called for a DQL UPDATE statement
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::postUpdateCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPostUpdateCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $objectId = $this->createCompositeIdentifier();
        $logger = $serviceManager->get('logger');
        $logArgs = ['app' => 'MUMUImageModule', 'user' => $serviceManager->get('zikula_users_module.current_user')->get('uname'), 'entity' => 'album', 'id' => $objectId];
        $logger->debug('{app}: User {user} updated the {entity} with id {id}.', $logArgs);
    
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_POST_UPDATE, $event);
    
        return true;
    }
    
    /**
     * Pre-Process the data prior to a save operation.
     * This combines the PrePersist and PreUpdate events.
     * For more information see corresponding callback handlers.
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::preSaveCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPreSaveCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_PRE_SAVE, $event);
        if ($event->isPropagationStopped()) {
            return false;
        }
    
        return true;
    }
    
    /**
     * Post-Process the data after a save operation.
     * This combines the PostPersist and PostUpdate events.
     * For more information see corresponding callback handlers.
     *
     * @see MU\MUImageModule\Entity\AlbumEntity::postSaveCallback()
     * @return boolean true if completed successfully else false.
     */
    protected function performPostSaveCallback()
    {
        $serviceManager = ServiceUtil::getManager();
        $objectId = $this->createCompositeIdentifier();
        $logger = $serviceManager->get('logger');
        $logArgs = ['app' => 'MUMUImageModule', 'user' => $serviceManager->get('zikula_users_module.current_user')->get('uname'), 'entity' => 'album', 'id' => $objectId];
        $logger->debug('{app}: User {user} saved the {entity} with id {id}.', $logArgs);
    
        $dispatcher = $serviceManager->get('event_dispatcher');
    
        // create the new FilterAlbumEvent and dispatch it
        $event = new FilterAlbumEvent($this);
        $dispatcher->dispatch(MUImageEvents::ALBUM_POST_SAVE, $event);
    
        return true;
    }
    
    
    /**
     * Returns the formatted title conforming to the display pattern
     * specified for this entity.
     */
    public function getTitleFromDisplayPattern()
    {
        $serviceManager = ServiceUtil::getManager();
        $listHelper = $serviceManager->get('mu_muimage_module.listentries_helper');
    
        $formattedTitle = ''
                . $this->getTitle();
    
        return $formattedTitle;
    }
    
    
    /**
     * Returns a list of possible choices for the workflowState list field.
     * This method is used for validation.
     */
    public static function getWorkflowStateAllowedValues()
    {
        $serviceManager = ServiceUtil::getManager();
        $helper = $serviceManager->get('mu_muimage_module.listentries_helper');
        $listEntries = $helper->getWorkflowStateEntriesForAlbum();
    
        $allowedValues = [];
        foreach ($listEntries as $entry) {
            $allowedValues[] = $entry['value'];
        }
    
        return $allowedValues;
    }
    
    /**
     * Returns a list of possible choices for the albumAccess list field.
     * This method is used for validation.
     */
    public static function getAlbumAccessAllowedValues()
    {
        $serviceManager = ServiceUtil::getManager();
        $helper = $serviceManager->get('mu_muimage_module.listentries_helper');
        $listEntries = $helper->getAlbumAccessEntriesForAlbum();
    
        $allowedValues = [];
        foreach ($listEntries as $entry) {
            $allowedValues[] = $entry['value'];
        }
    
        return $allowedValues;
    }
    
    /**
     * Sets/retrieves the workflow details.
     *
     * @param boolean $forceLoading load the workflow record.
     *
     * @throws RuntimeException Thrown if retrieving the workflow object fails
     */
    public function initWorkflow($forceLoading = false)
    {
        $currentFunc = FormUtil::getPassedValue('func', 'index', 'GETPOST', FILTER_SANITIZE_STRING);
        $isReuse = FormUtil::getPassedValue('astemplate', '', 'GETPOST', FILTER_SANITIZE_STRING);
    
        // apply workflow with most important information
        $idColumn = 'id';
        
        $serviceManager = ServiceUtil::getManager();
        $workflowHelper = $serviceManager->get('mu_muimage_module.workflow_helper');
        
        $schemaName = $workflowHelper->getWorkflowName($this['_objectType']);
        $this['__WORKFLOW__'] = [
            'module' => 'MUMUImageModule',
            'state' => $this['workflowState'],
            'obj_table' => $this['_objectType'],
            'obj_idcolumn' => $idColumn,
            'obj_id' => $this[$idColumn],
            'schemaname' => $schemaName
        ];
        
        // load the real workflow only when required (e. g. when func is edit or delete)
        if ((!in_array($currentFunc, ['index', 'view', 'display']) && empty($isReuse)) || $forceLoading) {
            $result = Zikula_Workflow_Util::getWorkflowForObject($this, $this['_objectType'], $idColumn, 'MUMUImageModule');
            if (!$result) {
                $session = $serviceManager->get('session');
                $session->getFlashBag()->add(\Zikula_Session::MESSAGE_ERROR, $serviceManager->get('translator.default')->__('Error! Could not load the associated workflow.'));
            }
        }
        
        if (!is_object($this['__WORKFLOW__']) && !isset($this['__WORKFLOW__']['schemaname'])) {
            $workflow = $this['__WORKFLOW__'];
            $workflow['schemaname'] = $schemaName;
            $this['__WORKFLOW__'] = $workflow;
        }
    }
    
    /**
     * Resets workflow data back to initial state.
     * To be used after cloning an entity object.
     */
    public function resetWorkflow()
    {
        $this->setWorkflowState('initial');
    
        $serviceManager = ServiceUtil::getManager();
        $workflowHelper = $serviceManager->get('mu_muimage_module.workflow_helper');
    
        $schemaName = $workflowHelper->getWorkflowName($this['_objectType']);
        $this['__WORKFLOW__'] = [
            'module' => 'MUMUImageModule',
            'state' => $this['workflowState'],
            'obj_table' => $this['_objectType'],
            'obj_idcolumn' => 'id',
            'obj_id' => 0,
            'schemaname' => $schemaName
        ];
    }
    
    /**
     * Start validation and raise exception if invalid data is found.
     *
     * @return boolean Whether everything is valid or not.
     */
    public function validate()
    {
        if ($this->_bypassValidation === true) {
            return true;
        }
    
        $serviceManager = ServiceUtil::getManager();
    
        $validator = $serviceManager->get('validator');
        $errors = $validator->validate($this);
    
        if (count($errors) > 0) {
            $session = $serviceManager->get('session');
            foreach ($errors as $error) {
                $session->getFlashBag()->add(\Zikula_Session::MESSAGE_ERROR, $error->getMessage());
            }
            return false;
        }
    
        return true;
    }
    
    /**
     * Return entity data in JSON format.
     *
     * @return string JSON-encoded data.
     */
    public function toJson()
    {
        return json_encode($this->toArray());
    }
    
    /**
     * Collect available actions for this entity.
     */
    protected function prepareItemActions()
    {
        if (!empty($this->_actions)) {
            return;
        }
    
        $currentLegacyControllerType = FormUtil::getPassedValue('lct', 'user', 'GETPOST', FILTER_SANITIZE_STRING);
        $currentFunc = FormUtil::getPassedValue('func', 'index', 'GETPOST', FILTER_SANITIZE_STRING);
        $component = 'MUMUImageModule:Album:';
        $instance = $this->id . '::';
        $serviceManager = ServiceUtil::getManager();
        $permissionHelper = $serviceManager->get('zikula_permissions_module.api.permission');
        $translator = $serviceManager->get('translator.default');
        if ($currentLegacyControllerType == 'admin') {
            if (in_array($currentFunc, ['index', 'view'])) {
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'display', 'arguments' => ['id' => $this['id']]],
                    'icon' => 'search-plus',
                    'linkTitle' => $translator->__('Open preview page'),
                    'linkText' => $translator->__('Preview')
                ];
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'admindisplay', 'arguments' => ['id' => $this['id']]],
                    'icon' => 'eye',
                    'linkTitle' => str_replace('"', '', $this->getTitleFromDisplayPattern()),
                    'linkText' => $translator->__('Details')
                ];
            }
            if (in_array($currentFunc, ['index', 'view', 'display'])) {
                if ($permissionHelper->hasPermission($component, $instance, ACCESS_EDIT)) {
                    $this->_actions[] = [
                        'url' => ['type' => 'album', 'func' => 'adminedit', 'arguments' => ['id' => $this['id']]],
                        'icon' => 'pencil-square-o',
                        'linkTitle' => $translator->__('Edit'),
                        'linkText' => $translator->__('Edit')
                    ];
                    $this->_actions[] = [
                        'url' => ['type' => 'album', 'func' => 'adminedit', 'arguments' => ['astemplate' => $this['id']]],
                        'icon' => 'files-o',
                        'linkTitle' => $translator->__('Reuse for new item'),
                        'linkText' => $translator->__('Reuse')
                    ];
                }
                if ($permissionHelper->hasPermission($component, $instance, ACCESS_DELETE)) {
                    $this->_actions[] = [
                        'url' => ['type' => 'album', 'func' => 'admindelete', 'arguments' => ['id' => $this['id']]],
                        'icon' => 'trash-o',
                        'linkTitle' => $translator->__('Delete'),
                        'linkText' => $translator->__('Delete')
                    ];
                }
            }
            if ($currentFunc == 'display') {
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'adminview', 'arguments' => []],
                    'icon' => 'reply',
                    'linkTitle' => $translator->__('Back to overview'),
                    'linkText' => $translator->__('Back to overview')
                ];
            }
            
            // more actions for adding new related items
            $authAdmin = $permissionHelper->hasPermission($component, $instance, ACCESS_ADMIN);
            
            $uid = $serviceManager->get('zikula_users_module.current_user')->get('uid');
            if ($authAdmin || (isset($uid) && isset($this->createdUserId) && $this->createdUserId == $uid)) {
            
                $urlArgs = ['album' => $this->id];
                if ($currentFunc == 'view') {
                    $urlArgs['returnTo'] = 'adminViewAlbum';
                } elseif ($currentFunc == 'display') {
                    $urlArgs['returnTo'] = 'adminDisplayAlbum';
                }
                $this->_actions[] = [
                    'url' => ['type' => 'picture', 'func' => 'adminedit', 'arguments' => $urlArgs],
                    'icon' => 'plus',
                    'linkTitle' => $translator->__('Create picture'),
                    'linkText' => $translator->__('Create picture')
                ];
            
                $urlArgs = ['parent' => $this->id];
                if ($currentFunc == 'view') {
                    $urlArgs['returnTo'] = 'adminViewAlbum';
                } elseif ($currentFunc == 'display') {
                    $urlArgs['returnTo'] = 'adminDisplayAlbum';
                }
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'adminedit', 'arguments' => $urlArgs],
                    'icon' => 'plus',
                    'linkTitle' => $translator->__('Create album'),
                    'linkText' => $translator->__('Create album')
                ];
            }
        }
        if ($currentLegacyControllerType == 'user') {
            if (in_array($currentFunc, ['index', 'view'])) {
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'display', 'arguments' => ['id' => $this['id']]],
                    'icon' => 'eye',
                    'linkTitle' => str_replace('"', '', $this->getTitleFromDisplayPattern()),
                    'linkText' => $translator->__('Details')
                ];
            }
            if (in_array($currentFunc, ['index', 'view', 'display'])) {
                if ($permissionHelper->hasPermission($component, $instance, ACCESS_EDIT)) {
                    $this->_actions[] = [
                        'url' => ['type' => 'album', 'func' => 'edit', 'arguments' => ['id' => $this['id']]],
                        'icon' => 'pencil-square-o',
                        'linkTitle' => $translator->__('Edit'),
                        'linkText' => $translator->__('Edit')
                    ];
                    $this->_actions[] = [
                        'url' => ['type' => 'album', 'func' => 'edit', 'arguments' => ['astemplate' => $this['id']]],
                        'icon' => 'files-o',
                        'linkTitle' => $translator->__('Reuse for new item'),
                        'linkText' => $translator->__('Reuse')
                    ];
                }
                if ($permissionHelper->hasPermission($component, $instance, ACCESS_DELETE)) {
                    $this->_actions[] = [
                        'url' => ['type' => 'album', 'func' => 'delete', 'arguments' => ['id' => $this['id']]],
                        'icon' => 'trash-o',
                        'linkTitle' => $translator->__('Delete'),
                        'linkText' => $translator->__('Delete')
                    ];
                }
            }
            if ($currentFunc == 'display') {
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'view', 'arguments' => []],
                    'icon' => 'reply',
                    'linkTitle' => $translator->__('Back to overview'),
                    'linkText' => $translator->__('Back to overview')
                ];
            }
            
            // more actions for adding new related items
            $authAdmin = $permissionHelper->hasPermission($component, $instance, ACCESS_ADMIN);
            
            $uid = $serviceManager->get('zikula_users_module.current_user')->get('uid');
            if ($authAdmin || (isset($uid) && isset($this->createdUserId) && $this->createdUserId == $uid)) {
            
                $urlArgs = ['album' => $this->id];
                if ($currentFunc == 'view') {
                    $urlArgs['returnTo'] = 'userViewAlbum';
                } elseif ($currentFunc == 'display') {
                    $urlArgs['returnTo'] = 'userDisplayAlbum';
                }
                $this->_actions[] = [
                    'url' => ['type' => 'picture', 'func' => 'edit', 'arguments' => $urlArgs],
                    'icon' => 'plus',
                    'linkTitle' => $translator->__('Create picture'),
                    'linkText' => $translator->__('Create picture')
                ];
            
                $urlArgs = ['parent' => $this->id];
                if ($currentFunc == 'view') {
                    $urlArgs['returnTo'] = 'userViewAlbum';
                } elseif ($currentFunc == 'display') {
                    $urlArgs['returnTo'] = 'userDisplayAlbum';
                }
                $this->_actions[] = [
                    'url' => ['type' => 'album', 'func' => 'edit', 'arguments' => $urlArgs],
                    'icon' => 'plus',
                    'linkTitle' => $translator->__('Create album'),
                    'linkText' => $translator->__('Create album')
                ];
            }
        }
    }
    
    /**
     * Creates url arguments array for easy creation of display urls.
     *
     * @return array The resulting arguments list.
     */
    public function createUrlArgs()
    {
        $args = [];
    
        $args['id'] = $this['id'];
    
        if (property_exists($this, 'slug')) {
            $args['slug'] = $this['slug'];
        }
    
        return $args;
    }
    
    /**
     * Create concatenated identifier string (for composite keys).
     *
     * @return String concatenated identifiers.
     */
    public function createCompositeIdentifier()
    {
        $itemId = $this['id'];
    
        return $itemId;
    }
    
    /**
     * Determines whether this entity supports hook subscribers or not.
     *
     * @return boolean
     */
    public function supportsHookSubscribers()
    {
        return true;
    }
    
    /**
     * Return lower case name of multiple items needed for hook areas.
     *
     * @return string
     */
    public function getHookAreaPrefix()
    {
        return 'mumuimagemodule.ui_hooks.albums';
    }
    
    /**
     * Returns an array of all related objects that need to be persisted after clone.
     * 
     * @param array $objects The objects are added to this array. Default: []
     * 
     * @return array of entity objects.
     */
    public function getRelatedObjectsToPersist(&$objects = []) 
    {
        foreach ($this->picture as $rel) {
            if (!in_array($rel, $objects, true)) {
                $objects[] = $rel;
                $rel->getRelatedObjectsToPersist($objects);
            }
        }
    
        return $objects;
    }
    
    /**
     * ToString interceptor implementation.
     * This method is useful for debugging purposes.
     */
    public function __toString()
    {
        return $this->getId();
    }
    
    /**
     * Clone interceptor implementation.
     * This method is for example called by the reuse functionality.
     * Performs a deep copy.
     *
     * See also:
     * (1) http://docs.doctrine-project.org/en/latest/cookbook/implementing-wakeup-or-clone.html
     * (2) http://www.php.net/manual/en/language.oop5.cloning.php
     * (3) http://stackoverflow.com/questions/185934/how-do-i-create-a-copy-of-an-object-in-php
     */
    public function __clone()
    {
        // If the entity has an identity, proceed as normal.
        if ($this->id) {
            // unset identifiers
            $this->setId(0);
    
            // reset Workflow
            $this->resetWorkflow();
    
            $this->setCreatedDate(null);
            $this->setCreatedUserId(null);
            $this->setUpdatedDate(null);
            $this->setUpdatedUserId(null);
    
            // handle related objects
            // prevent shared references by doing a deep copy - see (2) and (3) for more information
            // clone referenced objects only if a new record is necessary
            $collection = $this->picture;
            $this->picture = new ArrayCollection();
            foreach ($collection as $rel) {
                $this->addPicture( clone $rel);
            }
    
            // clone categories
            $categories = $this->categories;
            $this->categories = new ArrayCollection();
            foreach ($categories as $c) {
                $newCat = clone $c;
                $this->categories->add($newCat);
                $newCat->setEntity($this);
            }
            
        }
        // otherwise do nothing, do NOT throw an exception!
    }
}
