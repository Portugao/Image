{# purpose of this template: inclusion template for display of related album #}
{% set editImage = '<span class="fa fa-pencil-square-o"></span>' %}
{% set removeImage = '<span class="fa fa-trash-o"></span>' %}

<input type="hidden" id="{{ idPrefix }}" name="{{ idPrefix }}" value="{% if item is defined and item is iterable and item.id is defined %}{{ item.id }}{% endif %}" />
<input type="hidden" id="{{ idPrefix }}Mode" name="{{ idPrefix }}Mode" value="1" />

<ul id="{{ idPrefix }}ReferenceList">
{% if item is defined and item is iterable and item.id is defined %}
{% set idPrefixItem = idPrefix ~ 'Reference_' ~ item.id %}
<li id="{{ idPrefixItem }}">
    {{ item.getTitleFromDisplayPattern() }}
    <a id="{{ idPrefixItem }}Edit" href="{{ path('mumuimagemodule_album_' ~ routeArea ~ 'edit', { 'id': item.id }) }}">{{ editImage }}</a>
     <a id="{{ idPrefixItem }}Remove" href="javascript:muimageRemoveRelatedItem('{{ idPrefix }}', '{{ item.id }}');">{{ removeImage }}</a>
</li>
{% endif %}
</ul>
